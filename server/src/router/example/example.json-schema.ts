export const exampleSchema = {
    description: 'example schema',
    type: 'object',
    properties: {
        name: {
            description: 'name',
            type: 'string'
        }
    },
    required: ['name'],
};
